name: Github Actions Demo

run-name: ${{github.actor}} is trying out Github Actions with Linter

on: [push]

jobs:
  Explore-Github-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Job is run successfully"
      - run: echo "Job is triggered by ${{github.event_name}} event"
      - run: echo "OS - ${{runner.os}}, Branch name - ${{github.ref}}, repository - ${{github.repository}}"
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
         ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."